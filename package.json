{
  "name": "mil-prompt-server",
  "version": "0.1.0",
  "description": "Prompt engine for the daily-surface: always-on server timing engine that fires task prompts via SMS. Stage 0 transport test included.",
  "main": "send-test.js",
  "scripts": {
    "test-send": "node send-test.js",
    "engine": "node prompt-engine.js"
  },
  "dependencies": {
    "nodemailer": "^6.9.0",
    "@supabase/supabase-js": "^2.39.0"
  }
}